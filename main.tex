% =============================================================================
% NTUST (國立臺灣科技大學) Master's Thesis
% Built directly from the real "NTUST Thesis template 2.0.1 (Chinese Version)"
% by Horseisfish (Overleaf gallery: ntust-thesis-template-2-dot-0-1-chinese-
% version), source obtained as a zip export from the user. ntust_report.cls,
% common_env.tex, chinese_trans.tex, frontpages/ntust_frontpages.tex,
% backpages/ntust_backpages.tex, frontpages/NTUSTLogo.png, and watermark/ are
% all copied verbatim from that zip (only the opt-in sections this thesis
% actually needs -- symbol list, appendix, the two admin-form placeholder
% pages -- were uncommented; see each file's own comments for what changed
% and why). frontpages/my_*.tex, backpages/my_appendix.tex, and this file's
% own content-package list are this thesis's own content, not template.
%
% Two deliberate, explicit deviations from the template's literal defaults,
% both from earlier user decisions in this same migration:
%   1. Chapter numbering stays English ("Chapter 1"), not the template's
%      Chinese-numeral default ("第一章") -- chinese_trans.tex omits the real
%      source's \ctexset{chapter={name={第,章}, number=\chinese{chapter}}}.
%   2. Figure/Table caption labels stay English ("Figure"/"Table"), not the
%      template's default Chinese ("圖"/"表") -- explicit request.
% Everything else (watermark enabled, 3.5cm top margin, 1.4x line spacing,
% titletoc, algorithmic not algorithmicx, admin-form placeholder pages
% active, cover layout incl. student-ID rule under the degree line) matches
% the real template's own literal values, even where those differ from this
% thesis's earlier NTUST-regulation-driven values (regulation text says top
% margin 3cm and 1.5x spacing; the template itself uses 3.5cm/1.4x -- kept
% as the template has it per explicit "strictly follow the template"
% instruction; flagging here in case that should be revisited).
%
% Compile with XeLaTeX (CJK fonts require it):
%   xelatex main.tex && bibtex main && xelatex main.tex && xelatex main.tex
% =============================================================================

% !TEX program = xelatex
\documentclass[12pt, a4paper]{ntust_report}

\usepackage{fontspec}
\usepackage{xeCJK}
\usepackage[UTF8]{ctex}   % heading=true omitted -- see note above (English chapter numbering)

\setmainfont[Mapping=tex-text]{Times New Roman}
\setCJKmainfont[AutoFakeBold={4}]{cwTeXKai}
\XeTeXlinebreaklocale "zh"
\XeTeXlinebreakskip = 0pt plus 1pt

\setcounter{tocdepth}{3}
\setcounter{secnumdepth}{3}

\input{common_env}   % template's fixed environment (margins, page style, watermark, titletoc, etc.) -- do not alter

% ---- Packages the actual chapter content needs (not part of the template --
% common_env.tex already provides titletoc, cite, geometry, subfigure,
% multirow, graphicx, algorithmic, amsmath, amssymb, mathrsfs, listings, url,
% fancyhdr) --------
\usepackage{tabularx}
\usepackage{amsfonts}
\usepackage{comment}
\usepackage{rotating}
\usepackage{booktabs,threeparttable}
\renewcommand*{\thefootnote}{\fnsymbol{footnote}}
\usepackage{pdfpages}

\usepackage{adjustbox}
\usepackage{xcolor}
\usepackage{pifont}
\usepackage{placeins}
\usepackage{enumitem}
\usepackage{xspace}
\usepackage{titlesec}

\definecolor{cvprblue}{rgb}{0.21,0.49,0.74}
\usepackage[breaklinks,colorlinks,allcolors=cvprblue]{hyperref}

% report-class raggedbottom + float-page glue fix: pins a lone [t]-placed
% figure/table to the top of its page instead of centering it in a blank gap.
\raggedbottom
\makeatletter
\setlength{\@fptop}{0pt}
\setlength{\@fpbot}{0pt plus 1fil}
\makeatother

% ---- Font sizes per NTUST spec: chapter title 20pt centered; section/subsection 18pt left-aligned.
% ntust_report.cls itself only uses the relative size \LARGE for chapter heads (not literally
% 20pt at this 12pt base), so this overrides it explicitly.
\titleformat{\chapter}[display]
  {\centering\normalfont\fontsize{20pt}{28pt}\selectfont\bfseries}
  {\chaptertitlename\ \thechapter}{0.5em}{}
\titleformat{\section}
  {\raggedright\normalfont\fontsize{18pt}{24pt}\selectfont\bfseries}
  {\thesection}{0.5em}{}
\titleformat{\subsection}
  {\raggedright\normalfont\fontsize{18pt}{24pt}\selectfont\bfseries}
  {\thesubsection}{0.5em}{}

% Figures/tables are numbered per-chapter automatically by ntust_report.cls.

% ---- Method-specific macros carried over verbatim from the previous build ----
\newcommand{\todo}[1]{\textcolor{red}{[\textbf{TODO:} #1]}}
\newcommand{\tbd}{\textcolor{red}{$\ldots$}}
\newcommand{\method}{\textbf{Rotate-ID}\xspace}
\newcommand{\dupid}{\textbf{dup-id}\xspace}
\newcommand{\csim}{\textbf{csim}\xspace}
\newcommand{\gate}{$\gamma$-\textbf{gate}\xspace}
\newcommand{\cmark}{\textcolor{green!55!black}{\ding{51}}}
\newcommand{\xmark}{\textcolor{red!70!black}{\ding{55}}}
\newcommand{\pmark}{\textcolor{orange!95!black}{$\circ$}}

\begin{document}

\input{chinese_trans}   % section-heading label glossary (\nameAckn, \nameToc, ...) -- do not alter

%-----------------------------------------------------------------------------
% Front pages: cover, title page, Chinese/English abstract, acknowledgement,
% table of contents, list of figures, list of tables, list of symbols.
% Structure is fixed by the template (frontpages/ntust_frontpages.tex);
% actual content lives in frontpages/my_*.tex.
%-----------------------------------------------------------------------------
\input{frontpages/ntust_frontpages.tex}

%-----------------------------------------------------------------------------
% Main body
%-----------------------------------------------------------------------------
\input{sections/1_introduction.tex}
\input{sections/2_related_work.tex}
\input{sections/3_metrics.tex}
\input{sections/4_method.tex}
\input{sections/5_mechanism.tex}
\input{sections/6_experiments.tex}
\input{sections/7_limitations.tex}
\input{sections/8_conclusion.tex}

%-----------------------------------------------------------------------------
% Back pages: references, appendix. Structure is fixed by the template
% (backpages/ntust_backpages.tex); actual content lives in backpages/my_*.tex.
%-----------------------------------------------------------------------------
\input{backpages/ntust_backpages.tex}

\end{document}
